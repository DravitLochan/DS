#include <iostream>
#include <algorithm>
#include <map>
#define loop(i, x, y)	for(int i = int(x), i < int(y); ++i)
using namespace std;

int main(){
	int t;
	cin>>t;
	while(t--){
		int n, x, y, i;
		long long int sum = 0;
		map <int, int> m;
		cin >> n >> x >> y;
		int a[n], b[n];
		loop(i, 0, n){
			cin >> a[i];
		}
		loop(i, 0, n){
			cin >> b[i];
		}
		loop(i, 0, n){
			m[]
		}
	}
}